################################################################################
# Judge0 Configuration — dev defaults. Safe to commit: these are throwaway
# passwords used ONLY inside the private docker-compose network, never exposed
# to the host or internet. Change them before deploying anywhere public.
################################################################################

################################################################################
# PostgreSQL
################################################################################
POSTGRES_HOST=judge0-db
POSTGRES_DB=judge0
POSTGRES_USER=judge0
POSTGRES_PASSWORD=judge0-dev-password

################################################################################
# Redis
################################################################################
REDIS_HOST=judge0-redis
REDIS_PASSWORD=judge0-dev-redis-password

################################################################################
# Submission limits (isolate sandbox) — mirrors COMPILATION_TIMEOUT_MS /
# EXECUTION_TIMEOUT_MS in lib/compiler.ts, converted to seconds.
################################################################################
CPU_TIME_LIMIT=3
WALL_TIME_LIMIT=6
MEMORY_LIMIT=128000
MAX_PROCESSES_AND_OR_THREADS=60

################################################################################
# Queueing — cap on in-flight submissions before new ones are rejected.
# Revisit this under real load.
################################################################################
MAX_QUEUE_SIZE=200

################################################################################
# Security — submissions must not be able to reach the network.
################################################################################
ENABLE_NETWORK=false
